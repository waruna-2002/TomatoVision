import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF121212);
  static const Color cardBackground = Color(0xFF1E1E1E);
  static const Color borderMuted = Color(0xFF2C2C2C);
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFAAAAAA);

  // Quality Tier Colors
  static const Color tierUnripe = Colors.blue;
  static const Color tierFresh = Colors.green;
  static const Color tierOverripe = Colors.yellow;
  static const Color tierSpoiled = Colors.red;
}